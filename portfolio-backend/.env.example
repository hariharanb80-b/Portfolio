# Copy this file to .env and fill in your values
# Never commit .env to GitHub!

PORT=3000

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=portfolio

# Set a strong random string as your admin key
# Used to access GET /admin/messages
ADMIN_KEY=change_this_to_a_strong_secret_key
